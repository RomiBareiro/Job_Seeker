services:
  jobseeker:
    image: jobseeker
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 3000:3000
    environment:
      - Host=localhost
      - Port=5432
      - User=postgres
      - Password=yourpassword
      - DBName=jobseeker